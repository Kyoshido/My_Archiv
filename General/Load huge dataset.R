###############################################################################

# Load of packages ------------------------------------------------------------

library("ff")

# Load data -------------------------------------------------------------------

data  <- read.csv.ffdf(file = "data.csv", 
                       header = TRUE,
                       VERBOSE = TRUE, 
                       first.rows = 10000,
                       next.rows = 50000, 
                       colClasses = NA)

variable <- as.ram(data$column)

# -----------------------------------------------------------------------------

###############################################################################
